// import { cn } from "@/utils/cn";
import { Text, View } from "react-native";

type Props = {
  active?: boolean;
};

export default function Card({ active }: Props) {
  return (
    <View>
      <Text>AAAA </Text>
    </View>
  );
}
